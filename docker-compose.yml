services:
  ai-receptionist:
    build: .
    container_name: ai-receptionist
    restart: unless-stopped
    ports:
      - "127.0.0.1:8787:8787"
    env_file: .env
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
